void	v()
{
	char buffer[512];
	
	fgets(buffer, 512, STDIN);
	printf(buffer);

	int	*ptr = 0x804988c;
	if (*ptr == 64)
	{
		fwrite("Wait what?!\n", ...);
		system("/bin/sh");
	}	
}

int 	main()
{

	v();
	return 0;	
}
